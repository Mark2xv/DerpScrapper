using DerpScrapper.DBO;
using System.Windows.Forms;

namespace DerpScrapper
{
    public partial class FixMismatch : Form
    {
        public FixMismatch(Serie serie)
        {
            InitializeComponent();
            this.Icon = Resources.Resources.books; 
        }
    }
}
